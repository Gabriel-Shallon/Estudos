-----------------------------
CRUD é uma sigla, que vem de:

C = Create
R = Read
U = Update
D = Delete

O CRUD é muito usado em orientação objeto.
Nesse contexto, usamos as funções em orientação objeto:

Create = Quando criamos uma classe, também criamos um construtor (create) que irá realizar a criação desse objeto;
Read = Consegue carregar um arquivo do disco (ler um objeto);
Updadte = Atualiza os valores de um objeto;
Delete = Apaga um objeto;

As struct(s) em si não são objetos. A definição de uma struct é que ela é uma spécie de classe bem rudmentar, e uma instância de uma struct seria algo parecido com um objeto.